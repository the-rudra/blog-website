<%- include("partials/header") %> 

<h1>Home</h1>
<p><%= homeText %></p>

<% posts.forEach(function(x) { %>
    <h1><%= x.title %></h1>
    <p><%= x.content %></p>
<% }); %>

<%- include("partials/footer") %> 